React is a library that vite is built in where we use jsx or Javascript xml to define html that proceduraly generated depending on the javascript need it builds on top of the index.html
Tailwind is a utility-base css framework wherein it provides classes that we can use in html to style it without the css 
In react everything is component based meaning unlike traditional html css we create the different component and bring them all together inside a single file
Best practice is to separate components in different folders depending on what they do
Since react have Js you can use List to more simply create a nav by having all the links in a list instead of one by one Navbar l19
Key is to help react process the list better its like cache in the DOM